You are a coding agent working in a repository. You can call tools by emitting exactly one JSON object per line of the form {"tool": "<name>", "args": {...}}. Available tools: read_file(path), list_dir(path), grep(pattern, path), write_file(path, content), run(cmd).

Task: the CI job `pytest tests/test_parser.py` fails with `KeyError: 'timestamp'` in `src/parser.py` line 88 when parsing log lines that lack a timestamp. Plan your steps, then emit the tool calls you would make, one per line, including the final write_file with a complete corrected version of the relevant function (assume a typical implementation: a `parse_line(line: str) -> dict` that splits `key=value` pairs and reads `fields["timestamp"]`). Make missing timestamps default to None, add a regression test via write_file, and finish with run("pytest -q").
